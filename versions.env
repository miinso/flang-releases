# Centralized version definitions for flang release builds
# Source this file in workflows: source versions.env

LLVM_VERSION=21.1.8
TRACKED_LLVM_MINOR=21.1

# LLVM source defaults (can be overridden in workflows for patched fork refs)
LLVM_FORK_REPO=llvm/llvm-project
LLVM_FORK_REF=llvmorg-21.1.8

SCCACHE_VERSION=v0.12.0
NINJA_VERSION=1.13.1

# Emscripten SDK metadata.
# CI resolves effective emsdk/hash automatically from emsdk-llvm-map.json
# based on LLVM major, then verifies llvm-nm major matches expectation.
# These values remain as defaults for local use/documentation.
EMSDK_VERSION=3.1.74
EMSDK_HASH=c2655005234810c7c42e02a18e4696554abe0352

# NOTE: Using older emsdk for backward compatibility - flangrt built with older LLVM
# can be linked by newer wasm-ld, but not vice versa. Tested locally with 3.1.74.
